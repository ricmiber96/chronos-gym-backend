version: '3'
services:
  server:
    container_name: 'my-server-test'
    restart: always
    build: .
    ports:
      - "5000:5000"
    links:
      - mongo
    volumes:
      - .:/usr/src/app
  mongo:
    container_name: "my-database-test"
    image: mongo
    ports:
      - "27018:27017"
    volumes:
      - ./src/mongo/init-mongo-test.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
